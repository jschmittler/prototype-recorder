/node_modules/
dist/
# Anchor dev caches to specific paths — never use bare `.next/` or unanchored rules that
# hide Hostinger deploy output under hostinger-dist/.
/apps/web/.next/
/server.js
/storage/
/out/
/coverage/
*.tsbuildinfo

# env & secrets
.env
.env.local
.env.*.local
!.env.vps.example
*.pem

# job artifacts / local storage
/tmp-jobs/
*.webm

# os / editor
.DS_Store
*.log
